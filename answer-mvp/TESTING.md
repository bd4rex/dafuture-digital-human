# 问答功能、容量与稳定性测试

## 回归与 review 入口

```bash
npm test
npm run test:review
```

- 2026-09-15 发布前普通与严格回归均为 243/243、0 TODO；相对主持素材更新后的 228 项基线新增 15 项播报交互回归。此前四项恢复问题及测试清理竞态的 227 项基线见 [第三轮修复与复测报告](FIX_REVIEW_20260913_R3.md) / [English](FIX_REVIEW_20260913_R3.en.md)。[第二轮并发/取消修复](FIX_REVIEW_20260913_R2.md) 与 [第一轮格式/传输修复](FIX_REVIEW_20260913.md) 保留为历史记录，不代表所有场景无缺陷。
- `npm test` 执行全部普通回归，以及带 `REVIEW-*` 编号的问题断言。未来如暂存待修复问题为 `TODO`，它们**不计入通过数**；Node 在只有 TODO 失败时仍返回退出码 0，不能据此宣称项目无问题。
- `npm run test:review` 使用同一套测试，取消这些问题的 TODO 标记。任何待修复断言不满足都会以普通失败报告并返回非零退出码，适合作为修复验收门禁。
- 修复后须在严格模式通过对应断言，再移除该用例的 TODO 条件；不要删除复现用例或把错误行为改为通过标准。
- 当前 review 发现、复现命令与验证边界见 [2026-09-12 补测与审查](TEST_REVIEW_20260912.md) / [English](TEST_REVIEW_20260912.en.md)。

### 新增与强化的覆盖层

| 文件 | 测试内容 | 边界 |
| --- | --- | --- |
| `test/answer-lifecycle.test.js` | 主持接管/模式往返、真实 HTTP 取消、上游正文断开、健康与监听器恢复、下一轮回答 | 隔离模型适配器及两跳本机 HTTP；不承诺供应商内部计算/费用可撤销 |
| `test/client-events.test.js` | 并发同 ID 单写、持久化前不确认、失败可重试、不同 ID 独立 | 单服务进程内的去重窗口，不是跨重启永久去重 |
| `test/ops-log-recovery.test.js` | 真实 OS 部分写入失败与重试、残尾/UTF-8 字节恢复、轮转归档保留、恢复失败不确认 | 单写入进程；不自动修复历史内部坏行、不承诺断电持久性；OS 文件限额测试不适用于 Windows |
| `test/answer-functional.test.js`、`test/bundled-knowledge.test.js` | 核心 HTTP/预置知识服务先停服完成日志再删临时目录；关闭失败仍清理 | 确定性阻塞停机日志，不以反复重跑代替清理顺序断言 |
| `test/model-transport.test.js` | 两跳真实本机 HTTP：协议、401/429/503、断连、响应头/正文超时、截断、恢复与问答日志 | 使用本机模拟供应商，不调用外部模型 |
| `test/model-reliability.test.js` | 答案分段、格式与长度边界、配置并发/落盘失败、完整正文日志与凭据脱敏 | 模拟供应商输出用于契约验证，不代表实际供应商通过验收 |
| `test/knowledge-lifecycle.test.js` | 真实 multipart、中文原文件下载、重启、原子替换、删除和大库改写期间的证据一致性 | 保留真实解析与持久化，仅模拟付费模型 |
| `test/knowledge-pdf.test.js` | 实际 PDF 文字层、图片/空白页、扫描件 HTTP 上传失败回滚 | 没有 OCR；扫描件仍需先 OCR |
| `test/avatar-runtime.test.js` | 执行前台源码：播报期间草稿与重复操作守卫、输入法、指定男声锁定/加载/缺失、语音异常恢复、录音会话隔离；保留 SSE 恢复、明确取消、失败提示、队列确认与请求悬挂 | 隔离 VM 与假时钟，不验证真实设备可听见声音；队列仍有 200 条容量上限 |
| `test/workbench-runtime.test.js` | 执行工作台源码：编辑/知识回包交叉、保存/停止竞态、模型/知识响应头与正文截止、有界只读核对、坏响应/晚回包不清草稿且不自动重写 | 隔离 VM，不代替浏览器布局或真机验收；超时表示结果未知而非未执行 |

所有新增服务测试使用临时数据目录和占位凭据，监听 `127.0.0.1` 的随机端口，结束后关闭自己的服务并清理自己的目录。禁止用真实模型配置或正式知识目录替代测试夹具。容量测试、真机语音、真实模型效果需要单独执行，普通回归不会隐式触发。

### 2026-09-13 发布同步检查

本次同步第二、第三轮累计修复及测试：相对上一已发布 main `35f013c` 的 174 项，增加 53 项，总数 227。`answer-lifecycle`、`client-events`、`ops-log-recovery` 三个新增测试文件必须与已有前台、工作台和清理顺序回归一起提交；两种测试入口会自动发现这些文件，无需另装测试框架。

发布前执行 `npm test`、`npm run test:review`、`npm audit --omit=dev` 和 `git diff --check`；合并后在同步到远端 main 的代码上再执行严格测试。227 项无失败/取消/跳过/TODO 是本次 macOS/Node 24 环境的门禁结果，不是跨平台或真实供应商验收。历史报告中的“未推送”描述对应当轮修复时点，后续发布另记于仓库时间日志。

### 2026-09-15 播报交互发布同步检查

本次从 `79bdfb4` 的 228 项基线增加 15 项，总数 243。代码、`test/avatar-runtime.test.js`、中英文 README/外部模型交接文档和时间日志一起提交；普通与严格入口会自动纳入新增测试。发布前复跑两种测试及生产依赖审计，合并后在远端 main 对应代码上再次运行严格测试。

补充人工验收（TC-AVATAR-ROBUST-001）：

1. 对话正在生成、准备声音、实际播报时分别输入第二题，连续按 Enter、点击发送/麦克风/四态测试按钮；第一题不取消、不重复生成，第二题草稿保留。
2. 第一题结束后由用户发送第二题；不自动排队，不用中文输入法的确认键误提交。
3. 指定男声缺失时保留文字并提示；声音列表刷新不替换已选男声，不退回默认女声。
4. 语音失败、启动超时、丢失结束事件与主动静音均能恢复控件；后台主持停止/接管和控制断线仍执行原取消语义，迟到回调不能复活旧回答。
5. 在目标手机/微信内检查键盘弹出后的输入可见性、视频持续播放和实际音色；桌面 Chromium、模拟语音和 390×480 缩小视口结果不等于真机验收。

## TC-FUNC-001：核心问答链路

**目标**：验证访客问题经过真实 HTTP `POST /answer`、后台知识上下文和 OpenAI 兼容调用后，返回可显示、可播报的结构化答案。

**前置条件**：无需配置真实模型或 API Key；测试使用临时内容、临时模型配置和进程内模拟模型，不会改写正式 `content.json`。

**步骤**：

1. 启动临时问答服务。
2. 发送 `{"question":"请告诉我培训地点"}`。
3. 校验问答结果、知识匹配和上游模型请求。

**通过标准**：HTTP 200；`answered=true`；`answerStatus=answered`；`speechText` 与 `answer` 一致；命中导入的培训地点片段；访客返回中不出现 `source` 或 `references`。

```bash
npm run test:functional
```

## TC-LIVE-001：对话/主持双模式实时联动

**目标**：验证后台模式选项卡真正控制全部在线数字人前台，主持词原文可确定性下发，并与大模型问答互斥。

**前置条件**：先在管理页完成密码设置或登录；至少打开两个 `/avatar` 前台页面。

**步骤**：

1. 在后台切换到“主持模式”，确认两个前台的问题输入、快捷问题、麦克风和发送按钮均不可用。
2. 新建并保存两段主持词，依次点击播报，确认两个前台都逐字显示和朗读当前选中段落，第二段会立即打断第一段。
3. 点击停止，确认前台保持主持待命但不再播报；再切回“对话模式”，确认问答输入恢复。
4. 再次进入主持模式并播报，然后重启服务且不刷新前台页面；确认 SSE 重连后前台自动回到对话模式，且不会自动重播旧指令。
5. 播报时断开前台网络并点击后台停止，恢复网络后确认旧稿不会继续；延迟返回旧健康检查也不得打断后续新稿。
6. 模拟语音失败和静音，确认前台不显示“播报完成”，后台反馈与日志分别记录失败和静音。

**通过标准**：所有前台模式一致；主持内容与后台保存的原文完全一致；主持模式下 `POST /answer` 返回 HTTP 409；运行模式和最后指令不跨服务重启恢复，主持词仍持久化存在。

自动接口测试包含持久化、模式互斥、SSE 事件和重启语义：

```bash
npm test
```

## TC-OPS-001：运维日志、脱敏与轮转

**目标**：确认关键动作的响应状态和执行耗时可持久化追踪，同时日志不会成为新的凭据或业务正文泄露点。

**步骤**：

1. 执行一次主持词保存与播报，再发起一次错误密码登录。
2. 从后台“运维日志”按 `live` 和“成功”筛选，核对动作、请求编号、HTTP 状态、耗时、操作者和连接前台数。
3. 搜索登录失败记录，核对 `ADMIN_LOGIN_FAILED`，再下载 JSONL。
4. 检查文件权限，确认测试密码、主持词正文和 API Key 不出现；每轮提问/回答全文仅在登录后的日志中可见，按 turnId 关联过程。
5. 使用较小文件上限连续写入日志，确认生成轮转文件且最新记录仍可查询。
6. 分别模拟模型 401、429、500 和截断 JSON；确认日志保留 upstreamStatus/finishReason，访客收到自然兜底。断网对话恢复网络后补传前台事件。

**通过标准**：查询和下载接口仅允许已登录管理员或 Bearer 管理密钥访问；成功、拒绝和失败状态可区分；日志权限为 `0600`；敏感字段显示为 `[REDACTED]` 或完全不采集；轮转后文件数量不超过配置上限。

```bash
npm test
```

## PT-CAP-STAB-001：容量和稳定性

### 要回答的问题

- 单个 Node.js 问答服务在给定资源下，能同时处理多少个问答请求？
- 该水位下的成功率、RPS、P50/P95/P99、CPU 和 RSS 内存是多少？
- 持续运行后是否有内存持续增长、错误率上升、延迟恶化或服务掉线？

### 测试流程

1. **预热**：在第一个并发水位运行，结果不纳入容量结论。
2. **阶梯并发**：默认依次测试 10、25、50、100、200 个同时请求，首次越过阈值后停止加压。
3. **稳定性浸泡**：默认使用“最高已通过阶梯×70%”的并发度持续运行，比较前后半程的成功率和 P95，同时比较内存首尾。
4. **就绪复查**：浸泡结束后再检查 `/ready`，确认服务没有因压力退出或转为不可用。

脚本采用闭环虚拟用户：每个虚拟用户收到答案后立即发出下一问，因此比普通访客行为更严苛。

### 默认安全模式

```bash
npm run test:capacity
```

默认启动一个独立的本地问答进程和模拟模型：

- 模拟模型延迟为 `800—1000 ms`。
- 只采样问答服务进程的 CPU 和 RSS，不把压测器与模拟模型的资源算进去。
- 不读取或调用真实模型配置，不产生模型费用。
- 报告写入 `output/load-tests/capacity-*.json`，该目录已被 Git 忽略。

### 正式容量和稳定性验收

建议在与正式机同规格的测试机上运行，并把模拟延迟设为实际模型的 P50 附近：

```bash
LOAD_LEVELS=10,25,50,100,200 \
LOAD_STEP_SECONDS=60 \
STABILITY_SECONDS=1800 \
MOCK_LLM_DELAY_MS=2000 \
MOCK_LLM_JITTER_MS=500 \
MAX_P95_MS=4000 \
npm run test:capacity
```

这个配置包含 5 分钟阶梯测试和 30 分钟稳定性浸泡。如果最高阶梯仍通过，只能说明“至少可承载该并发”，需扩大 `LOAD_LEVELS` 直到出现第一个失败阶梯，才能夹出容量边界。

### 完整链路测试

完整链路需要测试环境已配置真实模型。该测试会产生模型调用和费用，且可能触发供应商限流，所以脚本要求显式开关：

```bash
LOAD_TARGET_URL='https://test.example.edu/answer' \
LOAD_HEALTH_URL='https://test.example.edu/ready' \
ALLOW_BILLABLE_LOAD_TEST=YES \
LOAD_LEVELS=5,10,20,40 \
LOAD_STEP_SECONDS=60 \
STABILITY_SECONDS=900 \
MAX_P95_MS=8000 \
npm run test:capacity
```

不应直接对正式环境执行。远程模式默认无法采集服务器 CPU/内存；如果压测脚本与问答进程在同一台主机，可设置 `LOAD_MONITOR_PID=<问答进程PID>`，否则应同时从 Docker/Kubernetes/云监控取得服务端资源曲线。

### 默认通过阈值

| 指标 | 默认阈值 | 可调环境变量 |
| --- | ---: | --- |
| 成功率 | ≥ 99% | `MIN_SUCCESS_RATE` |
| P95 | ≤ 2000 ms | `MAX_P95_MS` |
| 问答进程平均 CPU | ≤ 80% | `MAX_AVG_CPU_PERCENT` |
| 问答进程峰值 RSS | ≤ 512 MB | `MAX_RSS_MB` |
| 稳定性首尾 RSS 增长 | ≤ 64 MB | `MAX_RSS_GROWTH_MB` |
| 后半程成功率下降 | ≤ 1 个百分点 | `MAX_SUCCESS_RATE_DROP` |
| 后半程 P95 / 前半程 P95 | ≤ 1.5 | `MAX_P95_DRIFT_RATIO` |

对真实模型的 P95 阈值应按数字人的用户体验 SLA 调整，不应沿用本地模拟模式的 2000 ms。

### 如何得出“多少用户”和“多少资源”

- `highestPassingTestedConcurrency` 是本次实测已通过的最高“同时问答请求数”。
- `recommendedConcurrentRequests` 是稳定性浸泡通过的工作水位，默认为最高通过阶梯的 70%。对外承诺建议使用这个值，不使用压测极限。
- “同时在线用户”不等于“同时提问用户”。如果高峰时估计 10% 在线用户会同时提问，则可以用 `recommendedConcurrentRequests ÷ 10%` 粗估在线用户规模，但还必须受真实模型配额限制。
- 内存可先按稳定性阶段峰值 RSS 的 1.5 倍向上取整，脚本在 `suggestedMemoryFloorMb` 中给出下限，最低不小于 256 MB。
- CPU 应以稳定工作水位下长期低于 60%、尖峰低于 80% 为配置目标。单个 Node.js 进程如果接近单核瓶颈，单纯增加 vCPU 不一定线性提升，应考虑多实例、限流与排队。
- 外部大模型 API 的并发限额、TPM/RPM 和响应延迟往往会先于 Node.js 服务成为瓶颈；本地模拟结果只用于问答服务本身的资源规格。

### 常用参数

| 参数 | 默认值 | 含义 |
| --- | ---: | --- |
| `LOAD_LEVELS` | `10,25,50,100,200` | 阶梯并发数 |
| `LOAD_STEP_SECONDS` | `10` | 每个阶梯时长 |
| `LOAD_WARMUP_SECONDS` | `3` | 预热时长 |
| `STABILITY_SECONDS` | `60` | 稳定性浸泡时长 |
| `STABILITY_CONCURRENCY` | 自动 70% | 手动指定稳定性并发 |
| `MOCK_LLM_DELAY_MS` | `800` | 模拟模型基础延迟 |
| `MOCK_LLM_JITTER_MS` | `200` | 模拟模型随机额外延迟 |
| `MOCK_LLM_MAX_CONCURRENCY` | `0` | 模拟上游并发限额，0 表示不限 |
| `LOAD_REQUEST_TIMEOUT_MS` | `15000` | 单请求超时 |
| `LOAD_MONITOR_PID` | 空 | 要采样的本机问答进程 PID |
