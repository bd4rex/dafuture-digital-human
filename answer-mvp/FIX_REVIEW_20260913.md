[English](FIX_REVIEW_20260913.en.md)

# 五项 review 问题修复与复测 — 2026-09-13

## 结果

前次 review 的五项问题均已修复，原有 `REVIEW-*` 编号保留，TODO 标记全部移除。普通测试及严格 review 门禁均为 **174/174 通过，0 失败、0 TODO**。相比修复前 165 项，新增 9 项测试，并扩展原用例中的正反例。

本轮保持 Demo 的配置方式：不新增配置页面或同源限制，不改现有知识库、Key、管理密码和主持词，不新增工具执行流程。复测完成时改动尚未提交或推送，后续发布状态以项目时间日志及 GitHub main/PR 为准；本报告不代表已经部署。

## 修复内容

| 原问题 | 修复 | 回归与证据 |
| --- | --- | --- |
| `REVIEW-AVATAR-001` 前台请求悬挂 | 请求头和正文读取共用 140 秒最终截止；主动取消和超时分别处理；清理计时器与监听器；已取消的迟到响应不再开始读取正文 | 响应头/正文悬挂、140 秒边界、125 秒内合法长请求、主持接管、新问替换、迟到结果与下一轮恢复均通过 |
| `REVIEW-MODEL-001` 正常业务文本被误判为拒答 | 拒答启发式只识别明确的开头自述，不再全文搜索“资料不足”；短自定义兜底只精确匹配；结构化状态仍优先 | 材料补交、条件句、引用、部分未知但有有效事实、明确拒答及完整问答日志均通过 |
| `REVIEW-MODEL-002` 思考分段混入答案 | 仅接受 `type: text` 文本分段及原本兼容的字符串条目；思考和未知类型不提取 | 有/无最终文本、普通分段拼接、独立 `reasoning_content`、日志无思考正文均通过 |
| `REVIEW-MODEL-003` 工具过程文字被当作最终答案 | 有文本时只接受缺省/null/stop 的完成状态，且不能含实际工具调用；其余进入 `MODEL_RESPONSE_REJECTED` 自然兜底；空正文维持空响应分类 | tool_calls/function_call/未知续答状态、伪装为 stop 的工具元数据、空工具元数据兼容，以及结束原因日志均通过 |
| `REVIEW-HTTP-001` 正文断流被记成坏 JSON | 检查底层网络错误码，将中途断流归为 `MODEL_CONNECTION_FAILED`；超时优先，完整坏 JSON 与坏压缩响应保留格式错误 | 原生 fetch + 真实本机 HTTP 验证断流、响应头/正文超时、坏 JSON、坏 gzip、401/429/503 和恢复 |

140 秒是针对“网络一直挂起且不主动报错”的最终等待上限，覆盖服务端最长 120 秒模型调用、最多 5 秒改写和 15 秒传输余量。实际网络报错仍立即进入原有兜底，不必等满 140 秒；采用固定上限避免前台旧配置误杀后台刚调长的模型请求。超时日志使用 `CLIENT_REQUEST_TIMEOUT`，包含本轮编号、完整提问/兜底回答与耗时，仍仅后台可查。

关键实现位于 `public/avatar.js` 的 `requestAnswer`，以及 `server.js` 的 `extractMessageContent`、`isExplicitPlainTextRefusal`、`callLanguageModel`。

## 验证

- `npm test`：174 通过，0 失败/跳过/TODO，约 3.55 秒。
- `npm run test:review`：174 通过，0 失败/跳过/TODO，约 3.59 秒，退出码 0。
- `npm audit --omit=dev`：0 个已知依赖漏洞；不等于全面安全审计。
- JavaScript 语法检查与 `git diff --check`：通过。
- 独立交叉 review：传输/完成状态和前台取消/超时分别复核；未发现本轮范围内的阻断性问题。补充了取消后的迟到响应不再读取正文的保护和断言。
- 普通回归原始记录：本地忽略目录 `output/review-20260912/five-fixes-20260913.tap`。

## 真实浏览器复测

使用 Playwright 打开隔离本机服务，模型响应与浏览器语音事件为模拟：

1. “报名资料不足怎么办？”显示并送入语音的是完整材料补交说明，没有被替换为知识不足话术。
2. 混合思考/文本分段只播报“门票免费，欢迎参加。”。
3. 工具调用中间态只播报配置的自然兜底，未出现过程文字。
4. 分别模拟响应头、正文悬挂，超时后按钮恢复；释放迟到结果后，不增加播报且不显示迟到内容。
5. 恢复网络后可正常发送下一问；隔离日志保留 `CLIENT_REQUEST_TIMEOUT` 和完整问答正文。

浏览器验收将 140 秒计时器仅在测试页面加速为 700 毫秒；实际 140 秒/125 秒边界由统一假时钟的源码运行测试验证。测试钩子不写入业务代码。服务按最终代码重新启动后再次验证以上流程。

没有调用真实付费模型、没有验证物理扬声器或微信真机，也没有进行新一轮容量浸泡或 Docker 部署。测试通过证明已覆盖契约与故障处理，不代表所有供应商的实际回答质量或设备语音效果均已验收。

[修复前 review 存档](TEST_REVIEW_20260912.md) / [测试使用说明](TESTING.md)
