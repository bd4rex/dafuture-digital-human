# “大未来”数字人问答 MVP 最简方案

- **目标：** 后台准备好问答内容，前台传入问题，接口返回对应答案。
- **范围：** 只做问答接口，不做后台界面、复核、审批、数据库、向量库和大模型。

## 1. 方案

```text
content.json 问答内容 → answer-service 问答接口 → 前台 / 数字人
```

整个 MVP 只有两部分：

1. 一份 `content.json`，保存问题和标准答案。
2. 一个 `POST /answer` 接口，根据问题匹配并返回答案。

## 2. 内容格式

```json
[
  {
    "id": "project-introduction",
    "questions": [
      "什么是大未来项目？",
      "介绍一下大未来项目"
    ],
    "keywords": ["大未来", "项目", "介绍"],
    "answer": "这里填写经过整理的标准回答。",
    "source": "大未来项目说明"
  },
  {
    "id": "training-location",
    "questions": [
      "培训地点在哪里？",
      "去哪里参加培训？"
    ],
    "keywords": ["培训", "地点", "哪里"],
    "answer": "这里填写已经确认的培训地点。",
    "source": "培训通知"
  }
]
```

每条内容只需要四项：

- `questions`：用户可能提出的几种问法。
- `keywords`：用于匹配问题的关键词。
- `answer`：前台直接显示或由数字人直接播报的答案。
- `source`：可选，记录内容来自哪份材料。

## 3. 问答接口

请求：

```http
POST /answer
Content-Type: application/json
```

```json
{
  "question": "培训地点在哪里？"
}
```

匹配成功：

```json
{
  "answered": true,
  "answer": "这里填写已经确认的培训地点。",
  "speechText": "这里填写已经确认的培训地点。",
  "source": "培训通知"
}
```

没有匹配内容：

```json
{
  "answered": false,
  "answer": "当前内容中暂无相关信息。",
  "speechText": "当前内容中暂无相关信息。"
}
```

前台只需提交 `question`，使用返回的 `answer` 或 `speechText`。

## 4. 匹配规则

接口按以下顺序查找：

1. 与 `questions` 中某个问法完全一致，直接返回答案。
2. 没有完全一致时，根据 `keywords` 匹配最接近的一条。
3. 没有明显匹配时返回“当前内容中暂无相关信息”。

首版不让大模型自由生成，避免出现资料里没有的答案。

## 5. 内容更新

内容人员直接修改 `content.json`。

- 服务每隔几秒检查文件是否变化。
- 新文件格式正确时自动重新加载。
- 新文件格式错误时继续使用内存中的上一份内容，并记录错误。
- 不需要复核、审批、发布和回滚操作。

由于首版没有复核流程，写入 `content.json` 的答案必须由内容人员自行确认。原材料中的 `XXX`、`XX` 和冲突日期不要写入正式答案。

## 6. 最小部署

```text
answer-mvp/
├── server.js
├── content.json
├── package.json
└── Dockerfile
```

- 技术：Node.js + Fastify。
- 存储：本地 `content.json`。
- 运行：一个进程或一个 Docker 容器。
- 地址示例：`http://服务器地址:8080/answer`。
- 若需要公网访问，复用现有网关配置 HTTPS；不为 MVP 单独建设网关系统。

## 7. 实施时间

| 时间 | 工作 |
| --- | --- |
| 半天 | 完成内容文件读取、问题匹配和 `/answer` 接口 |
| 半天 | 整理首批问答、部署并与前台联调 |

正常情况下，**1 名开发人员 1 天可以完成**。

## 8. 验收

满足以下四项即可认为 MVP 已跑起来：

1. 前台能调用 `/answer` 并获得答案。
2. 已配置的问题能返回对应标准答案。
3. 未配置的问题返回“当前内容中暂无相关信息”。
4. 修改 `content.json` 后，新答案能够自动生效。

## 9. 后续需要时再增加

以后如果希望直接上传大量 Word、PDF，并回答没有预先整理过的问题，再增加文档解析、向量检索和大模型 RAG；当前 MVP 暂不考虑。
