[English](TIMESTAMP_LOG.en.md)

# 项目时间戳日志

## 2026-09-03 20:43 +0800

- 任务：依据《大未来数字人问答 MVP 最简方案 V1.2》建立首个可运行原型。
- 产出：新增 `answer-mvp/`，包含 Fastify 问答服务、示例 `content.json`、Dockerfile、自动测试和中文运行说明。
- 范围：仅实现确定性问答接口、内容文件校验和热加载；未加入后台界面、数据库、向量库、大模型、TTS 或数字人渲染。
- 验证：`npm test` 5 项全部通过；`npm audit --omit=dev` 未发现已知漏洞；本地真实 HTTP 请求通过精确命中、关键词命中、固定拒答和热更新验收；Docker 镜像成功构建，容器健康检查及容器内问答接口通过。
- 状态：原型已完成，尚未部署到正式服务器，未创建提交或推送远程仓库。

## 2026-09-03 21:10 +0800

- 任务：在确定性问答 MVP 上增加可供内容人员后续配置使用的 Web 界面。
- 产出：新增响应式内容工作台，可新增、修改、复制、排序、删除和搜索 FAQ，并可在同一页面即时测试答案；新增受保护的 `GET/PUT /api/content` 配置接口。
- 数据安全：继续使用 `content.json`，采用原子写入、版本冲突检查和外部文件变更检查；无管理密钥时只允许本机同源配置，远程配置支持 `ADMIN_API_KEY`。
- 验证：Node 自动测试 12 项全部通过；真实浏览器完成新增、填写、保存、即时问答闭环，桌面与 390px 移动视口检查通过，控制台 0 错误、0 警告；Docker 容器内保存及命名卷重建后的数据持久化通过。
- 状态：Web 原型完成，正式 `content.json` 未写入浏览器测试数据，尚未部署到正式服务器。

## 2026-09-03 22:35 +0800

- 任务：实现透明背景视频循环的数字人前台，并删除访客端的资料来源显示。
- 产出：新增 `/avatar` 响应式页面，以双 `<video>` 交叉淡入切换待机、思考、说话和主持四种姿态；配套 WebM Alpha 与 HEVC Alpha 演示素材、内置动画降级、浏览器语音联调和素材重建脚本。
- 问答：工作台可配置 OpenAI 兼容模型；API Key 仅在服务端以 `0600` 权限保存且不回显。访客前台只渲染回答文本，即使 API 额外返回 `source` 也不显示。
- 验证：`npm test` 21 项全部通过，`npm audit --omit=dev` 为 0 漏洞；Chromium 真实验证了 `thinking → speaking → idle` 问答流程、`presenting → idle` 主持流程、四段视频实际播放、故意携带资料来源的模拟响应不会泄露、桌面与 390px 移动端布局，控制台 0 错误、0 警告；WebM 透明通道取值已核验。
- 限制：本轮 Docker 守护进程未运行，因此没有重新构建镜像；正式上线前还需替换真人素材、配置实际模型 API，并根据环境接入生产级 TTS。

## 2026-09-03 22:38 +0800

- 任务：按新要求把确定性问答改为大语言模型驱动，并提供可在后台填写 API 地址、API Key、模型名和提示词的 Web 配置页。
- 产出：`POST /answer` 改为调用 OpenAI 兼容的 `/chat/completions`；`content.json` 改作模型知识上下文；新增模型配置读取、保存和连接测试接口，以及“仅依据后台内容/允许补充一般知识”两种回答范围。
- 密钥安全：模型设置单独原子写入被忽略的 `model-config.json`，文件权限为 `0600`；读取接口只返回 `hasApiKey`，从不回显 Key；留空保存保留旧 Key，可显式清除。
- 界面调整：工作台增加模型状态和模型设置弹窗，数字人前台同步显示模型生成状态；内容编辑、问答结果和示例内容中不再显示资料来源。
- 验证：Node 自动测试 21 项全部通过，`npm audit --omit=dev` 漏洞为 0；真实浏览器走通保存配置、Key 不回显、连接测试与生成回答，桌面和 390px 移动端可用且控制台 0 错误/0 警告；Docker 容器内模型调用、`0600` 权限及命名卷重启持久化通过。
- 状态：本机原型已在 `http://127.0.0.1:8080` 运行，当前保持“模型未配置”，等待使用者在页面中填入真实服务配置；测试假 Key、测试容器和测试卷均已清理。

## 2026-09-03 23:30 +0800

- 任务：将当前数字人项目发布到 GitHub，并设置为公开仓库。
- 仓库：`https://github.com/bd4rex/dafuture-digital-human`，默认分支为 `main`，可见性经 GitHub API 确认为 `PUBLIC`。
- 提交：首个公开内容提交为 `f5c9629065ea9ebbb38b53a241bfcd013aea309c`；包含方案文档、可运行原型、演示素材、中英文说明和验证记录。
- 公开前整理：忽略本机渲染目录、依赖、测试输出、环境文件、私钥和模型私有配置；移除生成脚本中的用户绝对路径；未提交真实 API Key。
- 验证：21 项自动测试全部通过，生产依赖审计为 0 漏洞，Markdown 相对链接全部可解析，提交内容未检出用户绝对路径、私网地址或常见真实密钥格式。

## 2026-09-04 06:38 +0800

- 任务：修复独立评审提出的模型配置切换、问答就绪状态、访客快捷问题配置和接口语义四类问题。
- 模型配置：完整的 API 地址、Key 或模型名发生变化时，服务先用候选配置测试连接，成功后才原子落盘并切换；候选失败不改变现有内存配置、磁盘文件或可用状态。只修改回答参数和提示词时无需重复连接测试。
- 健康状态：`GET /health` 增加明确的 `ready`、内容版本及模型连接状态；新增会按问答可用性返回 HTTP 200/503 的 `GET /ready`，Docker 健康检查改用该接口。成功与失败的真实模型调用会更新连接状态。
- 前后端衔接：访客快捷问题不再维护于独立静态配置，而是由当前 `content.json` 前三条内容的首个问法动态生成，并按内容 revision 在已打开的访客页自动刷新。
- 接口契约：`POST /answer` 新增结构化 `answerStatus` 与 `answerStatusSource`，保留 `answered` 兼容字段；用 `knowledgeContext.contextIds/matchedIds` 区分实际发送的上下文和启发式匹配，不再把它们表述为模型引用。
- 验证：Node 自动测试由 21 项扩展至 25 项并全部通过；隔离浏览器环境验证了候选错误配置回滚、旧配置继续可用、快捷问题无刷新同步、结构化回答、数字人 `thinking → speaking → idle` 流程以及 390px 视口无横向溢出，访客页控制台 0 错误、0 警告。
- 验证限制：本机 Docker 守护进程未运行，因此未重新构建镜像；`npm audit --omit=dev` 等待注册表 60 秒仍无结果后终止，本轮不声明新的依赖审计结论。
- 状态：修改仅存在于本地工作区，未创建提交、未推送 GitHub；浏览器验收使用临时内容目录和本地模拟模型，没有写入正式 `content.json` 或真实 API Key。

## 2026-09-04 15:20 +0800

- 任务：从老师交付的“默认、思考、对话、主持”四组素材中自行选择正式数字人视频，在保证透明背景和动作质量的前提下控制首屏与切换流量。
- 素材处理：选用四段 1080×1440、30 fps、QTRLE/ARGB 带通道 MOV 母版，总计约 1.48 GB；原文件保持原位且未修改。交付版统一为 720×960、30 fps、无音轨，并生成 VP9 Alpha WebM 与 HEVC Alpha MOV 两套浏览器格式。思考姿态使用前 3.9 秒正放加倒放的往返循环，消除原片首尾动作跳变。
- 体积结果：四段 WebM 合计 5.37 MiB，其中首屏默认姿态约 1.15 MiB；四段 Safari 用 MOV 合计 9.75 MiB。普通网络在默认姿态就绪后只预载思考姿态，进入思考后再预载对话姿态，主持姿态按需加载；省流量或 2G 网络只加载当前姿态。
- 前端实现：由四个持久化 `<video>` 层分别保存各姿态的已加载状态，并在切换时交叉淡入，避免返回旧姿态时再次从头下载；正式素材配置加入版本参数，保留内置动画降级。新增 `scripts/build-production-avatar.sh`，可从四个带通道母版重复生成并校验交付素材。
- 验证：视频均确认带有效透明通道且无音轨；Node 语法检查、转码脚本语法检查和 25 项自动测试全部通过。Chromium 实测四姿态播放、桌面与 390 px 手机布局、重复切换无新增媒体请求、省流量模式只请求默认姿态，以及模拟 API 的 `idle → thinking → speaking → idle` 全流程；故意返回的 `source` 字段及“资料来源”均未显示，控制台 0 错误、0 警告。
- 状态：正式数字人素材及加载策略已合并到 `main` 并同步 GitHub；本机服务运行于 `http://127.0.0.1:8080/avatar`，模型仍等待在内容工作台配置。

## 2026-09-04 16:39 +0800

- 任务：将当前浏览器播报从不确定的中文默认音色改为与男版数字人一致的普通话男声，并改善本机听感。
- 实现：在 `avatar-config.json` 增加 `speech` 配置，明确 `gender: male`，macOS/Chrome 优先选择 `Reed`，并提供 `Eddy`、`Rocko`、云希、云健、云扬、康康等跨平台男声候选；语速和音高均设置为 `0.98`。语音列表延迟就绪时会通过 `voiceschanged` 自动刷新首选音色。
- 验证：当前 Chrome 实际列出了 18 个中文音色；真实点击“主持开场”后捕获到 `Reed (中文（中国大陆）)`、`zh-CN`、rate `0.98`、pitch `0.98`，播报结束正常回到待机。JavaScript 语法检查和 25 项自动测试全部通过，浏览器控制台 0 错误、0 警告。
- 限制：浏览器 Web Speech 音色取决于终端安装情况；当前这台 Mac 已稳定为男声，但若正式部署要求所有设备音色完全一致，仍应改为服务端流式 TTS。
- 状态：男声修改当前仅存在于本地工作区，尚未创建提交或推送 GitHub；本机服务已重启并加载新配置。

## 2026-09-04 16:50 +0800

- 任务：在保留文字输入的同时增加语音提问，并为未来接入外部 ASR/TTS 模型保留清晰替换边界。
- 实现：输入框增加麦克风按钮；浏览器原生 `SpeechRecognition`/`webkitSpeechRecognition` 以 `zh-CN`、单轮、临时结果模式识别，最终文本自动复用原表单提交。监听期间显示动态按钮和状态提示；再次点击可提前结束。开始识别会停止当前播报，开始回答会停止识别，避免数字人把自己的声音再次录入。
- 降级：浏览器不支持时禁用麦克风但保留文字输入；权限拒绝、无麦克风、未听清和识别网络失败均给出对应提示并自动恢复。`speech.provider` 与 `speechInput.provider` 当前为 `browser`，未来外部驱动只需分别输出最终文本或可播放音频，现有问答和数字人状态机无需重写；供应商密钥继续只放服务端。
- 验证：25 项自动测试全部通过；当前 Chrome 确认原生语音识别接口可用。可控浏览器测试完成“临时识别 → 最终文本 → 自动提交 → thinking → speaking → idle”，问题、答案和 `Reed` 男声均正确；另行验证聆听/手动停止状态与拒绝麦克风权限后的恢复。桌面与 390 px 手机布局无溢出，四组浏览器控制台均为 0 错误、0 警告。
- 状态：男声与语音提问改进已合并到 `main` 并同步 GitHub；本机服务保持运行，外部 ASR/TTS 尚未绑定具体供应商或密钥。

## 2026-09-04 17:28 +0800

- 任务：将后台从仅编辑 `content.json` 升级为可导入外部文件、可持久化的知识库，并为管理界面增加简单但真正由服务端执行的密码验证。
- 知识库：支持 TXT、Markdown、CSV、JSON、DOCX 和 PDF；每次最多 10 个文件，单个 10 MB、合计 30 MB。原文件以 `0600` 权限保存在 `knowledge-files/`，提取文字和检索分片原子写入 `knowledge.json`；支持预览、下载、SHA-256 去重、追加、确认后替换和单文件删除。
- 问答衔接：导入文件被切分为可检索片段，服务根据问题选取最多 12 个相关片段，与手工问答共同送入大模型。文件名仅在后台显示，不作为访客端资料来源。
- 管理安全：未登录时管理地址只返回登录页，内容、知识库和模型接口均由服务端校验。首次密码只允许本机设置，仅落盘 scrypt 加盐哈希；登录使用默认 8 小时的 HttpOnly、SameSite=Strict 会话，服务重启和退出登录会使其失效。仍保留可选 `ADMIN_API_KEY` 供 Bearer 自动化调用。
- 验证：32 项 `npm test` 全部通过，包含 DOCX/PDF 文字提取、知识库重启恢复和登录/退出契约；`npm audit --omit=dev` 为 0 漏洞。隔离浏览器真实完成“未登录 → 密码登录 → 选择 Markdown → 导入并预览 → 重启服务 → 重新登录后继续可用”，桌面和 390 px 视口无横向溢出，控制台 0 错误、0 警告。
- 限制：本机 Docker 守护进程未运行，因此本轮未重新构建镜像；浏览器验收使用临时目录和专用测试密码，未改写正式 `content.json`、知识库或真实 API Key。

## 2026-09-04 19:47 +0800

- 任务：按 Demo 定位简化管理密码首次设置流程，取消“只能在服务器本机设置”和远程部署必须预设 `ADMIN_PASSWORD` 的要求。
- 实现：无论本机、局域网还是 Docker 访问，只要尚未创建管理密码，首次访问管理页即可直接设置；设置完成后仍由服务端会话保护内容、知识库和模型参数。`ADMIN_PASSWORD` 保留为可选预设方式。

## 2026-09-04 21:50 +0800

- 任务：继续简化 Demo 的首次密码设置，取消“只能从当前管理页面发起”的同源限制。
- 实现：系统尚无管理密码时，首次设置接口不再检查客户端地址或页面来源；密码建立后，登录和后台管理接口仍维持原有的密码、会话及同源保护。

## 2026-09-04 23:20 +0800

- 任务：实现可由后台实时切换的“对话模式 / 主持模式”，兼顾大模型交互的灵活性和现场播报的确定性。
- 后台控制：工作台新增双模式选项卡和主持词管理区，可新增、编辑、复制、排序、删除并持久化多段主持词；点击任一段即可把保存的原文实时下发给全部在线前台，也可统一停止播报或返回对话模式。
- 前台行为：对话模式继续使用现有 OpenAI 兼容模型、手工内容和导入知识库；主持模式禁用文字提问、快捷问题、麦克风和问答接口。新主持指令会打断上一段，停止后保持主持待命，返回对话模式后恢复问答。
- 运行边界：主持词原子写入独立的 `host-scripts.json` 并以 `0600` 权限保存；运行模式和最后一次播报指令只保存在内存中。服务重启后统一回到对话模式，已打开页面通过 SSE 重连自动同步且不会重播旧指令。
- 验证：34 项自动测试全部通过，`npm audit --omit=dev` 为 0 漏洞；真实浏览器以两个同时在线的数字人页面验证模式同步、逐字播报、连续指令打断、统一停止、问答恢复和无刷新重启恢复。管理页与数字人页在桌面和 390×844 手机视口均无横向溢出，控制台 0 错误、0 警告。
- 限制：本机 Docker 守护进程未运行，因此本轮未重新构建镜像；Dockerfile 和命名卷持久化路径已随功能更新。
- 状态：实现与验收已完成；主持词、管理密码、知识库和模型凭据等运行数据继续由 Git 忽略，不进入公开仓库。

## 2026-09-05 00:04 +0800

- 任务：增加面向运维排错的持久化日志系统，记录关键动作的响应结果、HTTP 状态和执行耗时，同时避免把业务正文或凭据写入日志。
- 记录范围：覆盖服务启动/停止、管理密码设置与登录/退出、手工内容保存、知识文件导入/下载/删除、模型设置与连接测试、主持词保存、模式切换、主持播报/停止、数字人问答和日志下载。每条记录包含请求编号、路由、状态码、耗时、操作者类型、客户端地址、User-Agent、结构化错误码及安全的计数/标识信息。
- 安全与持久化：不记录密码、API Key、Cookie、Authorization、系统提示词、问题/回答、知识正文或主持词正文；日志使用 `0600` 权限的 JSONL 文件，默认单文件 5 MiB、保留 3 个文件并自动轮转，可通过 `OPS_LOG_FILE`、`OPS_LOG_MAX_BYTES`、`OPS_LOG_MAX_FILES` 调整。写入异常会在健康状态中显示为降级，但不会阻断仍可用的业务请求。
- 管理界面：页头新增“运维日志”，支持按类别、执行结果、级别、关键词和数量筛选，显示存储状态、保留条数、命中数、占用空间、请求详情和错误码；可选每 5 秒自动刷新，并可下载完整 JSONL 供离线排查。
- 验证：37 项自动测试全部通过，`npm audit --omit=dev` 为 0 漏洞；隔离浏览器真实验证了桌面与 390×844 手机布局、主持动作记录、失败问答错误码、分类/关键词筛选和诊断展开，控制台 0 错误、0 警告。浏览器动作中的专用问题与主持词标记均未进入日志，文件权限确认为 `0600`。
- 限制：本机 Docker 守护进程未运行，因此未构建镜像；Dockerfile、命名卷路径和忽略规则已经包含运维日志持久化配置。

## 2026-09-05 06:35 +0800

- 任务：按 Demo 使用需求取消管理登录接口的页面来源校验。
- 实现：`POST /api/admin/login` 不再校验 `Origin` 与服务端 `Host` 是否一致；首次密码设置与后续登录均可从不同页面来源发起。密码校验、登录限流、HttpOnly/SameSite 会话和登录后的管理接口同源检查继续保留。
- 验证：自动测试使用不一致的 `Origin` 与 `Host` 验证错误密码仍返回 401、正确密码可登录并签发安全会话，同时登录后的跨源管理请求仍被拒绝。

## 2026-09-06 07:34 +0800

- 任务：按 Demo 定位撤除剩余的管理会话同源限制，并删除对话模式顶部占用空间的大标题。
- 访问调整：移除统一管理鉴权中的 `Origin`/`Host` 比对及 `ADMIN_ORIGIN_REJECTED` 分支；已登录会话现在可跨来源使用内容、知识库、模型、主持控制和运维日志接口。管理密码、登录限流、会话有效期与服务端权限校验继续保留。
- 界面调整：删除“把业务内容交给大模型，生成自然、可播报的回答。”大标题，并同步收紧介绍区上下留白；桌面实测介绍区高度由 216 px 降至 157 px。
- 验证：37 项自动测试全部通过；隔离 HTTP 服务使用不一致的 `Origin` 与 `Host` 完成登录、读取内容及切换主持模式，均返回 200 且识别为 `session`。真实浏览器确认大标题不再出现、页面无横向溢出，控制台 0 错误、0 警告。

## 2026-09-06 07:48 +0800

- 任务：将后台容易误解为固定答案配置的“问答条目”统一改为“知识库管理”。
- 界面调整：原有 `content.json` 条目现在显示为“人工知识”，字段统一为知识 ID、适用问题、检索关键词和知识内容；外部文件入口及弹窗统一显示为“文件知识”。右侧“问答试运行”继续保留，用于验证大模型结合整个知识库生成的回答。
- 兼容边界：本轮只调整管理语义和交互文案，不迁移数据，也不改变检索、持久化或大模型调用逻辑。现有 `id`、`questions`、`keywords`、`answer` 字段及已导入文件继续原样使用。
- 验证：37 项自动测试全部通过，`npm audit --omit=dev` 为 0 漏洞；隔离浏览器确认人工知识列表与编辑字段正常载入、“文件知识”弹窗可打开，桌面和 390×844 手机视口均无横向溢出，控制台 0 错误、0 警告。

## 2026-09-06 08:22 +0800

- 任务：实现自然回答与可配置兜底话术，让数字人正常回答更适合现场播报，并在知识不足或模型服务异常时稳定给出自然提示。
- 回答策略：后台模型设置新增“回答风格”“知识不足话术”“服务异常话术”。回答风格作为表达约束交给大模型；模型只判断能否可靠回答，知识不足时由服务端返回已配置话术，避免模型自行编造拒答内容。
- 异常处理：模型未配置、连接失败、超时、上游错误或返回内容无效时，接口保留真实 HTTP 状态和结构化错误码，同时向前台提供统一的服务异常话术；数字人页面会正常显示并播报该话术，方便访客理解，也不妨碍运维日志排错。
- 配置边界：只修改回答策略和界面配置，不改动现有知识库、运行数据或真实 API Key；仅调整风格或兜底话术时不会触发模型连接测试和外部调用。运维日志仍不记录回答风格、兜底话术、问题、答案或模型凭据。
- 验证：37 项自动测试全部通过，`npm audit --omit=dev` 为 0 漏洞；隔离真实浏览器分别验证正常回答、知识不足、模型服务错误三条路径，管理端与数字人端均使用对应自然话术，数字人播报状态可由 `speaking` 正常回到 `idle`。桌面和 390×844 手机视口均无横向溢出，控制台 0 错误、0 警告。
- 隔离说明：浏览器验收使用临时数据目录、临时管理密码和本地模拟模型服务；未写入正式 `content.json`、知识文件或任何真实 API Key，验收数据已移入废纸篓。

## 2026-09-06 09:33 +0800

- 任务：继续精简双模式后台，让对话模式直接进入知识库管理，并删除主持模式顶部占空间的大标题。
- 对话模式：移除人工问法录入、人工知识编辑、保存全部更改和后台问答试运行区域；文件知识库的导入、追加/替换、持久化状态、片段数量、预览、下载、删除和文件列表改为直接显示在主页面。原有 `content.json` 及 `GET/PUT /api/content` 仅保留兼容，不迁移或删除已有数据。
- 主持模式：删除“预先写好主持词，点击哪段，前台就播哪段。”大标题，并将顶部概览的间距、字号和高度与对话模式统一；主持词编辑、保存、实时播报、停止和返回对话功能保持不变。
- 验证：37 项自动测试全部通过，`npm audit --omit=dev` 为 0 漏洞；隔离实例导入 1 个 Markdown 知识文件后，真实浏览器确认文件数、片段数、持久化状态和文件列表即时显示，两个模式可正常切换，人工问题录入、后台试问和主持大标题均不再出现。1280 px 桌面视口无横向溢出，控制台 0 错误、0 警告。
- 隔离说明：验收只使用临时数据目录、临时管理密码和测试知识文件；正式 `content.json`、知识库、主持词和模型配置均未改动，验收数据已移入废纸篓。

## 2026-09-06 16:22 +0800

- 版本：`0.7.0`。修复知识换问法漏召回、主持控制竞态、异常模型 JSON 播报、隐藏知识、语音状态误报、日志诊断不足及断网无自然兜底问题。
- 知识：小知识库在 24,000 字符预算内全量送入模型；大知识库增加常见同义词检索与未命中回退，仍需后续用真实语料验证召回效果。只有后台列出的知识文件参与回答；历史 `content.json` 保留为不生效的备份，支持明确迁入为可预览、下载和删除的知识文件，停用隐藏样例快捷问题。
- 主持与语音：实时指令使用服务实例 ID 与递增序号，重连同步漏掉的停止指令，拒绝过期健康状态；控制连接中断时暂停旧播报，重连不自动重播。主持指令可打断晚到的对话回答，语音完成、失败、静音、取消和不可用分别显示并上报，增加语音启动超时检测。
- 模型与兜底：拒绝残缺 JSON、不合规范的结构及截断输出；`no_answer` 即使缺少 answer 字段也使用配置话术。模型 401、429、500、超时、连接失败、截断和解析异常可分别追踪；浏览器网络异常使用已加载的自然兜底话术进入语音链路。
- 日志：按用户明确选择保存完整提问和回答，仅已认证后台可查看、搜索和下载；对话编号关联接收、检索、模型耗时、结果及前台语音事件，并提供“查看本轮完整记录”。当前配置凭据继续脱敏。断网事件在当前标签页暂存最多 200 条，恢复后补传；日志仍按默认 5 MB × 3 文件轮转，不承诺无限保留。
- 验证：48 项自动测试全部通过，语法检查与 `git diff --check` 通过。隔离真实浏览器完成文件导入、同义提问、完整问答日志查看、断网自然兜底与恢复补传、主持语音失败反馈检查。模型使用本地模拟响应，语音使用浏览器事件模拟；未调用真实付费模型，也未验证实际扬声器音质或离线语音可用性。正式业务数据与模型凭据未改动；本轮未提交或推送 GitHub。
- 收尾：隔离验收实例与浏览器已关闭，临时测试数据移入废纸篓，可恢复；正式本地服务在 `127.0.0.1:8080` 启动并读回版本 `0.7.0`。现有模型配置仍为空，需管理员填写后才能进行真实问答验收。

## 2026-09-06 18:59 +0800

- 发布内容：按用户要求将 `0.7.0` 的知识检索、主持控制、模型响应校验、自然兜底与完整问答日志修复提交到 GitHub `main`。
- 发布目标：`https://github.com/bd4rex/dafuture-digital-human`，分支 `main`；发布前已拉取远端引用，本地与远端均位于 `1d1da33`，无分歧，无需额外分支合并。
- 发布验证：重新运行 48 项自动测试全部通过，检查变更格式、提交文件范围及敏感数据；同步中英文说明和版本记录。
- 数据边界：仅包含程序、测试与文档；模型配置、密钥、管理密码、知识库原文件、主持词运行文件及对话日志均不提交。本次 GitHub 更新不改变服务器部署或真实模型配置。

## 2026-09-06 19:54 +0800

- 任务：用新提供的“抠图版 6.0”母版替换数字人四状态资源，保留更细致的透明边缘并控制网页下载体积。
- 素材分配：按实际动作而非文件名称选取。新“对话模式”用于微笑待机；新“思考模式”的前 3.9 秒往返处理；新“默认姿势”的 1.5–5.0 秒有明显说话口型，用于对话和主持的两个独立状态。新“主持姿态”主要微笑眨眼，本轮未选用。四段交付时长分别约 4.07、7.8、3.5、3.5 秒；所有母版保持原样，循环片段不提供逐字口型同步。
- 体积与缓存：八个交付文件统一为 720×960、30fps、无音轨；VP9 Alpha WebM 合计 2,289,378 字节，较上一版减少约 64%；HEVC Alpha MOV 合计 5,884,929 字节，减少约 43%。全部素材地址更新为 `v=cutout-v6-20260906`，维持分状态按需加载及原有 TTS 音频驱动切换。
- 重建能力：构建脚本支持说话/主持片段的起点与时长参数，WebM CRF 调整为 30，HEVC Alpha 质量调整为 0.8；中英文素材说明记录本轮准确母版映射与可复现命令，防止再次按误导性的文件名接入。
- 验证：48 项自动测试、脚本语法检查及 `git diff --check` 通过。四个 WebM 经软件解码、四个 MOV 经 Apple AVFoundation 原生解码均确认真实透明和半透明像素；完成深浅背景边缘检查。隔离 Chrome 页面实测默认、思考、对话、主持及回切，四个视频均可播放且无媒体错误，控制台无错误/警告；`?preview=1` 四个测试按钮保留。
- 应用边界：本地 `127.0.0.1:8080` 服务已重启，读回新缓存版本，并验证八个素材均可按 Range 返回 206。未修改语音、模型和业务数据，未调用付费模型；未执行线上部署或宣称完成 Safari 浏览器实测，Safari 格式仅完成原生 Alpha 解码验证。

## 2026-09-06 22:59 +0800

- 任务与版本：`0.8.0`，将未来教师公益计划知识随应用发布并关联默认启动流程；发布目标为 `https://github.com/bd4rex/dafuture-digital-human` 的 `main`，工作分支为 `codex/bundled-future-teacher-knowledge`，起始提交为 `d1d088d`。
- 内容：27 份材料（10 份 PDF、17 份 DOCX）整理为 1 个项目知识库、7 份主题文件、89 张问答卡片；包含来源编号、原文件哈希、时间及版本差异。知识整理日为 9 月 6 日，具体研修安排采用 8 月 24 日盖章通知。完整原件和 OCR 中间资料保留在本地交付包，`outputs/` 排除出 Git。
- 关联与挂载：启动时校验清单及 SHA-256，追加导入且按内容去重；知识文件与 `appliedBundles` 记录同次原子持久化。已有资料与模型设置保留，后台删除、替换、清空后重启或更新镜像不会自动恢复。镜像源资料位于 `/app/bundled-knowledge`，运行数据位于 `/data`；缺少历史备份时初始化空 `content.json`。新增根目录 Compose 入口，支持首次自动创建持久化卷。
- 验证：56 项自动测试通过；58 道资料回归的必要证据全部进入上下文，当前 7 份文件生成 14 个片段并保持全量上下文。依赖审计 0 漏洞，文件哈希、27 个来源覆盖、中英文文档结构及变更格式检查通过。Docker 镜像构建成功；命名卷和可写主机目录绑定挂载均验证了首次自动导入、真实 HTTP 模拟模型问答、删除后重建不恢复，以及模型配置持久化。
- 验证边界：模型响应由隔离的 HTTP 模拟服务提供，没有调用真实付费模型；证据覆盖通过不代表真实模型回答准确率。模型凭据、管理密码、主持词运行文件、实际索引和日志均不进入仓库。全新部署仍需配置模型 API，已有模型配置的数据卷可直接沿用。

## 2026-09-06 23:04 +0800

- 发布完成：功能提交 `fb9e08e` 经 PR #1 合并至 GitHub `main`，合并提交为 `73f95a27de702fa55d34e673f6dbb47d1731c336`；本地已同步到同一提交，功能分支已清理。
- 本地关联：备份原空知识索引后重启当前服务，实际读回版本 `0.8.0`、7 份主题文件、14 个片段及持久化关联记录。知识已就绪；现有模型配置仍为空，填写模型 API 后方可生成真实回答。
- 收尾：命名卷与绑定挂载的临时验收容器、临时卷和目录已清理；验收使用的本地容器虚拟机恢复为停止状态。正常本地 Node.js 服务继续运行，原始资料交付包保留在本地。
